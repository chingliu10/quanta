     <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #f8f9fa;
            color: #333;
            line-height: 1.6;
        }


        .breadcrumb {
            background: #e9ecef;
            padding: 15px 25px;
            border-bottom: 1px solid #dee2e6;
        }

        .breadcrumb-item {
            display: inline;
            color: #6c757d;
            font-size: 14px;
        }

        .breadcrumb-item:not(:last-child)::after {
            content: " → ";
            margin: 0 8px;
        }

        .breadcrumb-item.active {
            color: #495057;
            font-weight: 500;
        }

        .form-container {
            padding: 30px;
        }

        .page-title {
            font-size: 24px;
            font-weight: 600;
            color: #212529;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 2px solid #007bff;
        }

        .section-title {
            font-size: 16px;
            font-weight: 600;
            color: #495057;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            display: block;
            font-weight: 500;
            color: #495057;
            margin-bottom: 8px;
            font-size: 14px;
        }

        .required {
            color: #dc3545;
        }

        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e9ecef;
            border-radius: 6px;
            font-size: 14px;
            transition: all 0.3s ease;
            background: white;
        }

        .form-control:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
        }

        .form-select {
            appearance: none;
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='m6 8 4 4 4-4'/%3e%3c/svg%3e");
            background-position: right 12px center;
            background-repeat: no-repeat;
            background-size: 16px;
            padding-right: 40px;
        }

        .amount-input-group {
            position: relative;
            display: flex;
            align-items: center;
        }

        .amount-controls {
            position: absolute;
            right: 5px;
            display: flex;
            flex-direction: column;
            z-index: 2;
        }

        .amount-btn {
            background: #007bff;
            color: white;
            border: none;
            width: 30px;
            height: 20px;
            font-size: 12px;
            cursor: pointer;
            transition: background-color 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .amount-btn:hover {
            background: #0056b3;
        }

        .amount-btn:first-child {
            border-radius: 3px 3px 0 0;
        }

        .amount-btn:last-child {
            border-radius: 0 0 3px 3px;
        }

        .toggle-group {
            display: flex;
            background: #f8f9fa;
            border-radius: 6px;
            padding: 4px;
            margin-top: 8px;
        }

        .toggle-option {
            flex: 1;
            padding: 8px 16px;
            text-align: center;
            cursor: pointer;
            border-radius: 4px;
            transition: all 0.2s ease;
            font-size: 14px;
            font-weight: 500;
        }

        .toggle-option.active {
            background: #007bff;
            color: white;
        }

        .toggle-option:not(.active) {
            color: #6c757d;
        }

        .toggle-option:hover:not(.active) {
            background: #e9ecef;
        }

        .notify-methods {
            display: flex;
            gap: 10px;
            margin-top: 8px;
        }

        .notify-methods.hidden {
            display: none;
        }

        .custom-fields-section {
            background: #f8f9fa;
            border: 2px dashed #dee2e6;
            border-radius: 6px;
            padding: 20px;
            text-align: center;
            margin: 20px 0;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .custom-fields-section:hover {
            border-color: #007bff;
            background: #f0f8ff;
        }

        .custom-fields-text {
            color: #6c757d;
            font-size: 14px;
        }

        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 123, 255, 0.4);
        }

        .btn-primary:active {
            transform: translateY(0);
        }

        .form-actions {
            margin-top: 40px;
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #e9ecef;
        }

        .optional-section {
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid #e9ecef;
        }

        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }
            
            .container-fluid {
                margin: 10px;
                max-width: calc(100% - 20px);
            }
        }
    </style>
    <br/>
    <div class="app-content">
        <div class="container-fluid">
            <!-- Breadcrumb -->
            <div class="breadcrumb">
                <span class="breadcrumb-item">Home</span>
                <span class="breadcrumb-item active">Add Repayment</span>
            </div>

            <!-- Form Container -->
            <div class="form-container">
                <h1 class="page-title">Add Repayment</h1>

                <form id="repaymentForm">
                    <!-- Repayment Amount -->
                    <div class="form-group">
                        <label class="form-label">
                            Repayment Amount <span class="required">*</span>
                        </label>
                        <div class="amount-input-group">
                            <input type="number" class="form-control" id="repaymentAmount" step="0.01" min="0" placeholder="0.00" required>
                            <div class="amount-controls">
                                <button type="button" class="amount-btn" onclick="adjustAmount(1)">+</button>
                                <button type="button" class="amount-btn" onclick="adjustAmount(-1)">-</button>
                            </div>
                        </div>
                    </div>

                    <!-- Repayment Method -->
                    <div class="form-group">
                        <label class="form-label">
                            Repayment Method <span class="required">*</span>
                        </label>
                        <select class="form-control form-select" id="repaymentMethod" required>
                            <option value="cash" selected>Cash</option>
                            <option value="bank_transfer">Bank Transfer</option>
                            <option value="check">Check</option>
                            <option value="mobile_money">Mobile Money</option>
                            <option value="card">Card Payment</option>
                        </select>
                    </div>

                    <!-- Collection Date -->
                    <div class="form-group">
                        <label class="form-label">
                            Collection Date <span class="required">*</span>
                        </label>
                        <input type="date" class="form-control" id="collectionDate" required>
                    </div>

                    <!-- Auto Apply Late Repayment -->
                    {{!-- <div class="form-group">
                        <label class="form-label">
                            Automatically Apply Late repayment to preceding schedules? <span class="required">*</span>
                        </label>
                        <div class="toggle-group">
                            <div class="toggle-option active" onclick="toggleOption(this, 'autoApply', 'no')">No</div>
                            <div class="toggle-option" onclick="toggleOption(this, 'autoApply', 'yes')">Yes</div>
                        </div>
                        <input type="hidden" id="autoApply" value="no">
                    </div> --}}

                    <!-- Notify Borrower -->
                    {{!-- <div class="form-group">
                        <label class="form-label">
                            Notify Borrower? <span class="required">*</span>
                        </label>
                        <div class="toggle-group">
                            <div class="toggle-option active" onclick="toggleNotify(this, 'no')">No</div>
                            <div class="toggle-option" onclick="toggleNotify(this, 'yes')">Yes</div>
                        </div>
                        <input type="hidden" id="notifyBorrower" value="no">

                        <!-- Notify Method (hidden by default) -->
                        <div class="notify-methods hidden" id="notifyMethods">
                            <label class="form-label" style="margin-top: 15px; margin-bottom: 8px;">
                                Notify Method <span class="required">*</span>
                            </label>
                            <div class="toggle-group">
                                <div class="toggle-option active" onclick="toggleOption(this, 'notifyMethod', 'email_sms')">Email & SMS</div>
                                <div class="toggle-option" onclick="toggleOption(this, 'notifyMethod', 'sms')">SMS</div>
                                <div class="toggle-option" onclick="toggleOption(this, 'notifyMethod', 'email')">Email</div>
                            </div>
                            <input type="hidden" id="notifyMethod" value="email_sms">
                        </div>
                    </div> --}}

                    <!-- Optional Fields Section -->
                    <div class="optional-section">
                        {{!-- <div class="section-title">Optional Fields</div> --}}

                        <!-- Description -->
                        <div class="form-group">
                            <label class="form-label">Description</label>
                            <textarea class="form-control" id="description" rows="4" placeholder="Enter any additional notes or description..."></textarea>
                        </div>

                        <!-- Custom Fields -->
                        {{!-- <div class="form-group">
                            <label class="form-label">Custom Fields</label>
                            <div class="custom-fields-section" onclick="addCustomField()">
                                <div class="custom-fields-text">
                                    <strong>Click here to add custom fields on this page</strong>
                                </div>
                            </div>
                            <div id="customFieldsContainer"></div>
                        </div> --}}
                    </div>

                    <!-- Form Actions -->
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{{> flash_message}}