<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Branches</title>
    {{> toplinks }}
</head>
<body>
    <div class="d-flex">
        {{> sidebar }}

        <div class="flex-grow-1">
            
            {{>topnav user=user.user}}

            <div class="container mt-4">
                <h2>Branches<a href="/branch/add" class="btn btn-primary">Add New Branch</a></h2>

                <!-- Error Message -->
                {{#if error}}
                <div class="alert alert-danger">{{error}}</div>
                {{/if}}

                <!-- Branches Table -->
                <table id="branches-table" class="table table-striped">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Users</th>
                            <th>Notes</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each branches}}
                        <tr>
                            <td>{{this.branch_name}}</td>
                            <td>{{this.user_count}}</td>
                            <td>{{this.branch_notes}}</td>
                            <td>
                                <div class="dropdown">
                                    <button 
                                        class="btn btn-sm btn-secondary dropdown-toggle" 
                                        type="button" 
                                        id="actionMenu" 
                                        data-bs-toggle="dropdown" 
                                        aria-expanded="false"
                                    >
                                        Action
                                    </button>
                                    <ul class="dropdown-menu" aria-labelledby="actionMenu">
                                        <li><a class="dropdown-item" href="/branch/details/{{this.branch_id}}">Details</a></li>
                                        <li><a class="dropdown-item" href="/branch/edit/{{this.branch_id}}">Edit</a></li>
                                        <li><a class="dropdown-item text-danger" href="/branch/delete/{{this.branch_id}}">Delete</a></li>
                                    </ul>
                                </div>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    {{> bottomlinks }}
</body>
</html>
